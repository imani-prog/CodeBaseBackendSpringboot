spring.application.name=CodeBaseBackend

# Spring Datasource Configuration

spring.datasource.url=jdbc:postgresql://localhost:5432/medilinkdb
spring.datasource.username=postgres
spring.datasource.password=Timo9380
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server Port (optional)
server.port=8080

# Security (JWT/Secret Example, change as needed)
# jwt.secret=ChangeThisSecretKeyForJWT

# Logging Level
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG

# Caching (Optional)
# spring.cache.type=redis

# Internationalization (Optional)
# spring.messages.basename=messages

# Cloud Storage / File Uploads (Optional)
# file.upload-dir=/var/medilink/uploads

# Email/SMS/Third-party Integrations (Optional)
# spring.mail.host=smtp.example.com
# spring.mail.username=your_email@example.com
# spring.mail.password=your_email_password

# Additional properties as needed for modules